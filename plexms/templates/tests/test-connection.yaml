apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "plexms.fullname" . }}-test-connection"
  labels:
    {{- include "plexms.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "plexms.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
