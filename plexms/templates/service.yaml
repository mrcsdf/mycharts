apiVersion: v1
kind: Service
metadata:
  name: {{ include "plexms.fullname" . }}
  labels:
    {{- include "plexms.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "plexms.selectorLabels" . | nindent 4 }}
